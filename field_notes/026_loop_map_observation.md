# Field Note 026: Loop Map Observation

Date: 2026-06-14

## Current Line

V13 initially appeared as a footer/reporting prompt.

It could ask for:

```text
V12 State
V13 Next Loop Gate
Reason
Next Loop Command
```

But the judgment core behind those labels was not yet explicit enough.

The risk was that V13 could look like formatting discipline instead of operational decision discipline.

## Target Line

V13 should become a judgment-routing layer.

It should help agents select next-loop gates from operational references, not from vibes or label habit.

The target line is:

```text
footer/reporting prompt
→ judgment references
→ routed gate selection
→ external behavior proof
→ contrastive value proof
```

## Required Intermediate Nodes

The repaired required node sequence is:

1. Required Intermediate Node / next 0.01 definition
2. V12→V13 mapping
3. CAP axis and limit selection
4. Aspire / Carrier / Re-entry proxies
5. Footer-axis consolidation
6. AGENTS.md judgment-reference routing
7. Internal routing test
8. External routing test

This line was not a full tree.

It was the minimum sequence needed to move V13 from footer format toward judgment routing.

## Filled Nodes

Filled:

- `field_notes/021_required_intermediate_node.md`
- `field_notes/022_v12_to_v13_mapping.md`
- `field_notes/023_cap_axis_limit_selection.md`
- `field_notes/024_aspire_carrier_reentry_operational_definitions.md`
- `field_notes/025_footer_axis_consolidation.md`
- AGENTS.md routing commit: `249ae99 Add AGENTS judgment reference routing`
- internal AGENTS routing real-task test
- external V12 repo routing test

## What Increased

The loop now has more operational structure:

- next 0.01 is no longer arbitrary
- V12 PASS no longer automatically produces V13 GO
- CAP is no longer "be careful"; it needs axis and limit
- Aspire / Carrier / Re-entry have observable proxies
- footer types are no longer treated as automatically additive
- AGENTS.md now routes judgment to references before labeling
- external repo test showed PASS → CAP behavior outside V13

This is a visible compounding line.

Each repaired node made the next repaired node more useful.

## What Did Not Increase Yet

The following are still not proven:

- public value
- ordinary-agent vs Decision-OS contrast
- stable-doc or schema-level maturity
- automation/pluginization readiness
- star-worthiness

The references are still field notes, not stable docs or schemas.

The system is stronger internally, but external value still needs contrastive proof.

## Current 1.01 Evidence

The external V12 repo test produced the current 1.01 evidence.

A normal completion-cost README improvement could have led to more polishing.

V13 instead routed through field notes 021–025 and produced:

```text
V12 State:
PASS

V13 Next Loop Gate:
CAP
```

The decision was:

```text
Keep V12 README edits stopped unless a real external reader reports one specific confusion.
```

That showed the repaired logic working outside V13:

- V12 PASS did not automatically become V13 GO
- the next missing node was evidence, not more editing
- CAP limited the loop to read-only evaluation
- footer inflation was avoided

## Next Missing Node

The next missing node is contrastive judgment proof.

Create one contrastive judgment example:

```text
ordinary agent likely says GO
Decision-OS says CAP/HOLD
```

The example should show the reasoning chain:

- current state
- target state
- earliest missing required node
- V12→V13 mapping
- CAP axis or HOLD reason
- Aspire / Carrier / Re-entry check
- footer choice

This is the next required node because public value is hard to see until the contrast is visible.

## Boundary

This is observation, not promotion.

This does not claim public readiness.

This does not claim plugin readiness.

This does not modify AGENTS.md again in this loop.

This does not update AGENTS.ja.md, README.md, docs, schemas, examples, prompts, plugin files, CI, or automation.

This does not add features, hooks, skills, MCP, pluginization, automation, outreach, posting, or product behavior.

## V13 Signal

Signal:
🟢 BLUE / LOOP-MAP-OBSERVED
+
🟢 BLUE / REQUIRED-NODES-FILLED-021-025
+
🟢 BLUE / EXTERNAL-PASS-NOT-AUTO-GO-EVIDENCE
+
🟡 YELLOW / PUBLIC-VALUE-NOT-PROVEN
+
🟡 YELLOW / CONTRASTIVE-JUDGMENT-STILL-MISSING

V12 State:
PASS

V13 Next Loop Gate:
CAP

Reason:
The repaired 0.01 nodes created a visible line from footer prompt toward judgment routing, including internal and external routing tests. Public value still needs contrastive proof before promotion or outreach.

Next Loop Command:
Create one contrastive judgment example: ordinary agent says GO, Decision-OS says CAP/HOLD.
