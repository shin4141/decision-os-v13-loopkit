# CLAUDE.md

This repository uses `AGENTS.md` as the canonical operating rule set.

Claude Code users should read and follow:

```text
AGENTS.md
```

Use this file as the Claude Code entry point.

If `CLAUDE.md` and `AGENTS.md` ever conflict, `AGENTS.md` wins.

## What this means

V13 LoopKit is not limited to Codex.

It is designed for AI coding workflows where an agent can follow project-level instructions.

For Claude Code, the intended path is:

```text
CLAUDE.md
↓
AGENTS.md
↓
V12 / V13 / Chat Continuation / Context Compression reporting
```

## Do not duplicate rules here

Do not copy the full `AGENTS.md` content into this file.

Keep the canonical rules in `AGENTS.md`.

This avoids drift between Codex-oriented and Claude Code-oriented entry points.

## First action

Claude Code users:

1. Keep this `CLAUDE.md` in the project root.
2. Read and follow `AGENTS.md`.
3. Use the V13 Lite Footer at the end of task reports.
4. Use Roadmap Anchors before selecting 0.01 repairs.

## One-line rule

`CLAUDE.md` is the Claude Code doorway.

`AGENTS.md` is the rulebook.
