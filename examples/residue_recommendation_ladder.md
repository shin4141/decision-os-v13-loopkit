# Residue Recommendation Ladder Example

Use this shape when a coding agent detects reusable residue and should avoid asking a heavy open-ended Owner question.

```text
Detected reusable residue:
The quickstart now explains when to read each memory surface, which may reduce repeated context rediscovery in future fork-user onboarding.

Recommendation:
High

Expected effect:
Recording this as an example can help future coding agents present bounded residue choices instead of asking "Should I add this?"

Suggested placement:
examples

Owner choices:
A. Record the minimal residue
B. Skip for now
```

`Owner Approval Required` is needed for `AGENTS.md` promotion, new files, automation, routing, or canonical rule changes.

