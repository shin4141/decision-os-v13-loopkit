# V13 Creator-Live Historical AGENTS Candidate Delta v0.9

Status: `AUTHORIZED — additive non-live implementation and fixture/fake qualification only`

## Purpose

This Forward-only Delta fixes the additive candidate:

```text
CREATOR_LIVE_AGENTS_BEFORE_AFTER_V0_2
```

It adds a content-addressed historical Before, a dedicated no-path source
capability, source-isolation and independence receipts, literal future tasks,
candidate gates, common-Before comparison and positioning projections, and a
fixture-only public-bundle assembler.

It assigns no Cycle and grants no live, proof-opening, model, task-transmission,
real-After, real-behavior, publication, release, or external-claim authority.

Candidate v0.1 remains unchanged and separately asks whether V13 can safely
compress an already compressed high-density instruction file.

## Authority and Starting State

Authorization was observed at `2026-08-05T23:39:00Z`.

The implementation starts only from:

| Field | Exact value |
| --- | --- |
| Repository | `/Users/sn/Documents/v13/decision-os-v13-loopkit` |
| Starting revision | `09d4c83c056f549da8feabd5588445e7437a4e01` |
| Branch | `main` |
| Local main / origin/main | exact equality |
| Ahead / behind | `0 / 0` |
| Tracked worktree and index | clean |
| Git operation | none |

Pre-existing untracked files are preserved. A mismatch returns `HOLD`; it does
not authorize reset, cleanup, repair, migration, or deletion.

Cycle 005 remains:

```text
FAILED / A3_REUSE / A3_EXACT_STRUCTURE_MISSING
```

Its fixed identities remain:

| Field | SHA-256 |
| --- | --- |
| Journal | `1de2e998804f5fb694707846b7deb0dc9d8b5f9cfc6027ad0210ddc270029322` |
| Anchor | `e246757a7ba98849a6b4a694ababf473dc1a98baf1fc1ce0ea7daa3a6e7e8610` |
| Typed readback | `481be90dc8751bda3d7b00714f5a0c650230dffa8974a1332881ce42c127710f` |

No Cycle 005 byte may be retried, resumed, replaced, migrated, normalized,
rewritten, reinterpreted, or deleted.

## Candidate Separation

The following identity and every existing v0.1 file remain immutable:

```text
CREATOR_LIVE_AGENTS_BEFORE_AFTER_V0_1
```

Candidate v0.2 does not supersede, rename, or canonicalize v0.1. Shared behavior
suite and rubric bytes may be referenced only by their existing identities.

## Fixed Common Before

| Field | Exact value |
| --- | --- |
| Logical artifact | `AGENTS.md` |
| Candidate-visible path | `before/AGENTS.md` |
| Source revision | `21cd88d4efb378a60cd08a28712083d9d4a8bc19` |
| Git blob | `f85b0d9b17a8f90a7128ea96d9c8f63a88022128` |
| UTF-8 bytes | `20705` |
| LF-based lines | `517` |
| SHA-256 | `e856160413a9d47622779dede6a2eeca9fd027284d815b155ab6e323a74863db` |
| Final LF | required |
| Normalization | forbidden |

The exact bytes are packaged at:

```text
decision_os/companion/candidate_inputs/creator_live_agents_before_after_v0_2/AGENTS.md
```

Non-live qualification binds the resource to the historical Git blob. Future
runtime loads only the installed resource and performs no Git or working-tree
lookup. Drift fails before thread start; fallback is forbidden.

## Dedicated Source Capability

The only source tool is:

```text
read_candidate_historical_before_v0_2
```

Its exact input schema is `{"type":"object","additionalProperties":false,
"maxProperties":0}`; it has no path parameter. The only valid successful
response contains exactly the fixed text, virtual path `before/AGENTS.md`,
source revision, Git blob, UTF-8 byte count, line count, and SHA-256. No other
response field or source is allowed.

Future Run 1 exposes exactly this source tool and
`propose_field_note_candidate`. The generic repository reader, native reader,
shell, Git, file mutation, web, MCP, plugins, apps, hooks, remote plugins,
multi-agent operation, attachments, and dependency installation are absent.

First valid call discloses once. Same-call replay returns the cached identical
response without a second disclosure. Another call ID fails
`SOURCE_ALREADY_CONSUMED`; non-empty arguments fail `SOURCE_ARGUMENTS_INVALID`.
Resource drift prevents thread start. Missing source, proposal-before-source,
or a prohibited capability event fails isolation.

## Isolation and Independence

Schemas:

```text
decision-os.creator-live-agents-source-isolation.v0.1
decision-os.creator-live-agents-independence.v0.1
```

Vocabulary:

```text
PASS / FAIL / NOT_ESTABLISHED
```

Only terminal `PASS` permits durable A1 admission. Positive violations are
`FAIL`. Missing, malformed, incomplete, unsupported, or unexcludable evidence
is `NOT_ESTABLISHED`. On either `FAIL` or `NOT_ESTABLISHED`, the memory-only
proposal is discarded and no durable A1 Note is admitted.

The receipts bind the Candidate and Contract identities, Run 1 task,
developer instructions, complete tool manifest, runtime, isolation features,
candidate-visible input set, source identity and counts, repository/Git/current-
After/prohibited-event counts, proposal ordering, normal terminal state,
content-free event digest, manual-After checks, reason codes, and canonical
receipt hashes.

The common Before is excluded from contamination scanning. Candidate-visible
control material must contain no manual-After SHA, blob, revision, byte/line
identity, Before-to-manual-After diff, After-only substantive line, or receipt
field carrying those values.

Independence establishes supplied-input and capability isolation only. It does
not claim absence of latent model memory.

## Fixed Future Tasks

Task hashing covers every byte including final LF.

| Task | Lane | Bytes | Lines | SHA-256 |
| --- | --- | ---: | ---: | --- |
| `prompts/creator_live_agents_before_after_v0_2_run_1.txt` | `A1_ONLY` | `2713` | `68` | `2ed80098fb169313b13c36dddfd69a3ab487a4fe31d8474889cff7ba441b09e2` |
| `prompts/creator_live_agents_before_after_v0_2_run_2.txt` | `EXACT_A2_ONLY` | `2703` | `56` | `1a67c3677ce8c73b4259317130e689dfefa1827fffe3abe377af861da8ec4bdb` |

This Delta records but does not transmit them. Neither task may contain the
generated Witness or manual-After material.

Run 1 reads the fixed source once, proposes once, makes no write, produces one
shorter projection with B01–B10 and one substantive generated Witness, then
stops. The proposal remains memory-only until terminal isolation and
independence `PASS`.

Run 2 receives only the exact durable A1 Note through exact A2 plus content-free
PASS receipts. It reproduces the designated Witness exactly once in its private
output, makes no write, and creates no publication authority.

## Projection, Compression, and Safety

The v0.1 public After projection algorithm and schema are inherited unchanged.
Strict UTF-8, LF-only structure, exact marker extraction, final LF, and no
normalization remain mandatory.

Candidate v0.2 compression uses:

```text
before_utf8_byte_count = 20705
PASS iff after_utf8_byte_count < 20705
```

There is no percentage threshold. Reduction fraction is exact byte reduction
divided by `20705`, rendered to six decimal places with decimal
`ROUND_HALF_UP`. Diff algorithm/version, additions, deletions, changed lines,
and LF-based line counts are deterministic.

The v0.1 public-safety patterns and vocabulary are inherited unchanged:

```text
PASS / FAIL / HUMAN_REVIEW_REQUIRED
```

Only `PASS` permits A2. This is not publication approval.

## B01–B10 Historical Boundary

The locator, regex-group, uniqueness, case, and public-safe-span semantics are
unchanged. Source rationales are refixed to the historical Before:

| ID | Historical source rationale |
| --- | --- |
| B01 | lines 240–275: operator final Seat and bounded continuation signals |
| B02 | lines 95–120: identity, ownership, freshness, validity, authority, and no substitution |
| B03 | lines 349–374 and 488–494: evidence promotion, rollback, and safety stops; candidate Contract adds protected-artifact guards |
| B04 | lines 21–63 and 127–175: restartable handoff, owned next action, closing responsibility |
| B05 | lines 31–37, 89–91, 101–120: HOLD/CAP/BLOCK under missing proof or prerequisite |
| B06 | lines 21–29 and 95–125: completion evidence, provenance, verified identity, bounded recovery |
| B07 | lines 21–63 and 127–177: restart state, Completion Line, next safe action |
| B08 | lines 129–175 and 240–275: bounded agent reporting and human final decision |
| B09 | lines 101–120: bounded routine recovery remains with the executing agent |
| B10 | lines 21–29 and 367–374: rollback/recheck/downgrade; Contract adds Forward-only repair and protected-history preservation |

Exactly one `PRESENT` result is required for every boundary. `MISSING` or
`AMBIGUOUS` fails. This is textual/structural evidence only.

## Witness and A3 Winner Binding

The locator is `A3 Witness: `. Run 1 generates the suffix. The complete line is
absent from both tasks, unique in the durable Note and projection, substantive,
at least 32 non-whitespace UTF-8 bytes, public-safe, and neither a nonce nor the
whole artifact. Matching is byte-exact without normalization or fuzzy meaning.

The durable binding is content-free: Candidate, exact Note identity digest,
projection digest, locator, byte count, Witness SHA, offsets, counts, and policy
result. Raw Witness text is private and transient unless separately approved.

The generic v0.3 A3 compiler and predicate remain unchanged. The Candidate
overlay additionally requires exactly one eligible/winning candidate, exact
durable source offsets, exact Witness SHA, byte-identical output span, and one
output occurrence. Another line cannot win.

## Post-A1 Admission

Schema:

```text
decision-os.creator-live-agents-post-a1-gate-readback.v0.2
```

Before A2, canonical durable readback must bind PASS receipts for source,
isolation, independence, projection, strict compression, B01–B10, safety,
Witness, and absence of private projection metadata. Issuance recomputes every
derivable receipt from the same exact Note and source. Mixed receipts fail.

Failure terminalizes before Run 2. Repair, retry, second proposal, alternate
source, and alternate Note are forbidden.

## Behavior Boundary

The inherited suite remains:

| Field | Identity |
| --- | --- |
| Suite schema | `decision-os.creator-live-agents-behavior-suite.v0.1` |
| Suite SHA-256 | `655fab6e1de937cc0057af2e5236ce38f07bb19deeb97143655082b2d45522b6` |
| Rubric SHA-256 | `553b372340570a211969588fd0114a497846c493171fa9e75494ce8965c705a1` |
| Scenarios | `10` |
| Threshold | `10/10` |

Fixtures may qualify only the harness. Candidate artifact behavior remains
`NOT_RUN`. Real `PASS` or `FAIL` requires separate fixed runtime and evaluator
authorization.

## Common-Before Comparison

Schema:

```text
decision-os.creator-live-agents-common-before-comparison.v0.1
```

The canonical root fields are `schema`, `comparison_result`, `shared_before`,
`lanes`, `claim_boundary`, and `cross_lane_isolation`. Current result is
`NOT_ESTABLISHED`.

Lane A Compactor fields remain `NOT_ESTABLISHED`; no identity, version, input
binding, result, metric, or qualification is invented.

Lane B fixes the common Before and the historical manual result: `11147` bytes,
`359` lines, SHA-256
`bb14c77c6b45c6bf365902b47729b455df566fa98688956824e072c352f2dae7`,
`9558` bytes and `158` lines removed, fraction `0.461628`, approximately
`46.16%`. Its class is canonical/manual textual restructuring only. B01–B10
and behavior receipts remain `NOT_ESTABLISHED`.

Lane C fixes the common Before. Generated fields remain `NOT_ESTABLISHED` until
a future authorized live attempt. Retry/replacement is `NOT_AUTHORIZED`.

Run 1 receives only the common Before. Run 2 receives only Lane C's exact Note
and receipts. The comparison is assembled afterward from public content-free
evidence. Raw Lane B After and diff bytes never enter Candidate execution.

## Reduction-Boundary Map

Schema:

```text
decision-os.reduction-boundary-map.v0.1
```

The exact core statement is:

> These systems are not competitors on one compression axis. They remove waste from different operational surfaces.

The map includes RTK/tool-output, Ponytail/implementation,
Compactor/persistent-instruction, OSI/output-handoff, and V13/iteration-authority
surfaces. Each entry fixes removal, preservation, combination, and verification
fields. External entries are `LATER_WEB_VERIFICATION_REQUIRED`; V13 is an
internal positioning statement. No external number, cross-layer ranking, or
superiority claim is admissible.

## Public Bundle v0.2

Schema and assembler:

```text
decision-os.creator-live-agents-before-after-public-bundle.v0.2
decision-os.creator-live-agents-before-after-public-bundle-assembler.v0.2
```

Fixture-only file set:

```text
before/AGENTS.md
after/AGENTS.md
manifest.json
diff.patch
boundary-checklist.json
source-isolation.json
independence-qualification.json
behavior-qualification.json
comparison-manifest.json
reduction-boundary-map.json
proof-summary.json
README.md
```

The allowlist contains fixed public artifact identities and metrics, safe
boundary spans, safety/isolation/independence/behavior result classes,
allowlisted receipt hashes, output and A3 identities, schema identities, claim
boundaries, and source recovery.

It excludes journals, anchors, raw readbacks, proof/run/Note identities, task
bodies, raw Note/output, raw Witness, approvals, provider configuration,
private paths, and unallowlisted historical identities. Fixture Witness bytes
require an explicit fixture-only approval flag. No real Witness publication is
approved.

## Claim Boundary

- Exact identities plus the fixed algorithm establish byte reduction only.
- B01–B10 establishes specified textual/structural presence only.
- Behavior preservation requires separately authorized real 10/10 evidence.
- A3 establishes exact designated-Witness reuse only.
- Isolation establishes supplied-input/capability isolation only.
- Independence does not exclude latent model memory.
- Loop governance does not establish output quality.
- Public usefulness and cross-tool superiority are not established.
- Shorter or a higher percentage does not mean a better or superior system.
- A common Before does not make lane objectives identical.
- V13 is not only a compressor.

## Authorized Additive Surface

Only the fifteen new files named in the implementation authorization may be
created. No existing production file, Candidate v0.1 file, `AGENTS.md`, generic
A3 file, transport, Contract semantic source, Cycle 005 file, or historical
artifact may change.

## Qualification and Closure

Qualification is fixture/fake-only. It includes exact source/task/schema tests,
isolation and failure cases, projection/compression/boundary/safety/Witness/A3
tests, behavior-harness separation, comparison/map/bundle determinism and
privacy, v0.1 and Cycle 005 hash guards, focused and full discovery, compilation,
JSON parsing, diff checks, independent Charter and full-diff review, canonical
build/install, packaged-resource inclusion, source/installed equality,
installed-product tests, fake process replay, process qualification,
Forward-only Contract refix, and exact-final-merge non-live P0.

P0 must not assign a Cycle, create/open proof storage, invoke a model, transmit
a task, generate a real After, run real behavior qualification, publish, or
release.

## Stop Conditions

Return `HOLD` or `BLOCK` if any starting/protected identity drifts; packaged
source differs; current/manual After exposure is required; generic reader or
unattested capability remains; an existing production file must change;
generic A3 must weaken; private text must be public; behavior PASS requires a
model; source/installed equality, Contract refix, or process qualification
fails; or Cycle/proof/model/task/publication/release activity appears necessary.

## Rollback

Before any future proof opening, abandon unmerged work or use a normal Forward
revert after merge. Rebuild/install the exact revert, Forward-only refix the
unchanged Contract, and re-establish source/installed equality and process
qualification. Candidate v0.1, Cycle 005, and historical proof bytes remain
unchanged.

## Completion Line

Stop after exact-final-merge non-live P0 with Candidate v0.2 fixed, Candidate
v0.1 and Cycle 005 unchanged, no Cycle assigned, no proof opened, no model or
task activity, no real After, artifact behavior `NOT_RUN`, comparison
`NOT_ESTABLISHED`, and no publication or release.
