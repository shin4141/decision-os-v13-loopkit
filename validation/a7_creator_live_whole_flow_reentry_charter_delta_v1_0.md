# V13 Creator-Live Cycle 006 Fixation Charter Delta v1.0

Status: `AUTHORIZED — additive non-live implementation and qualification only`

## Purpose and Authority

This Forward-only Delta assigns exactly one numeric identity:

```text
Cycle 006
cycle-006
CREATOR_LIVE_AGENTS_BEFORE_AFTER_V0_2
```

Shin's non-live implementation authorization was observed at
`2026-08-06T00:50:00Z`. It authorizes fixation, fixture/fake qualification,
review, merge, unchanged-Contract refix, canonical build/install,
source/installed equivalence, process qualification, and exact-final-merge P0.

It does not authorize clicking or invoking Start Cycle 006; opening or creating
proof storage; allocating an attempt; invoking a model; transmitting either
task; generating a real After; running real artifact behavior qualification;
retrying, replacing, reopening, migrating, or reinterpreting Cycle 005;
changing either Candidate; publication; release; or external claims.

Live-start authorization is explicitly `ABSENT`. The implementation stops with
Cycle 006 `READY / P0`, unopened and unconsumed.

## Parent Lineage and Starting Gate

This Delta is additive after
`validation/a7_creator_live_whole_flow_reentry_charter_delta_v0_9.md`, SHA-256
`ce9b60a1c54b7aff7a6083687ed4ffe8b52a7011d7eac64c339d525265c670b6`.
Every earlier Creator-Live Charter and Delta remains byte-immutable.

Implementation began only after read-only confirmation of:

| Field | Required identity |
| --- | --- |
| Repository | `/Users/sn/Documents/v13/decision-os-v13-loopkit` |
| Starting HEAD | `9c7cfc3a828062fcc863a306c1f55480f030b131` |
| Branch | `main` |
| Local main / origin/main | exact equality |
| Ahead / behind | `0 / 0` |
| Tracked worktree / index | clean |
| Git operation | none |
| Cycle 006 proof root | absent |

Pre-existing untracked files are preserved. A mismatch authorizes only `HOLD`,
not cleanup, reset, repair, migration, deletion, or substitution.

## Fixed Common Before

The Cycle binds only the installed content-addressed resource:

```text
decision_os/companion/candidate_inputs/
creator_live_agents_before_after_v0_2/AGENTS.md
```

| Field | Exact value |
| --- | --- |
| Logical artifact | `AGENTS.md` |
| Candidate-visible path | `before/AGENTS.md` |
| Revision | `21cd88d4efb378a60cd08a28712083d9d4a8bc19` |
| Git blob | `f85b0d9b17a8f90a7128ea96d9c8f63a88022128` |
| UTF-8 bytes | `20705` |
| Lines | `517` |
| SHA-256 | `e856160413a9d47622779dede6a2eeca9fd027284d815b155ab6e323a74863db` |
| Final LF | required |
| Normalization | forbidden |

Runtime lookup from working-tree bytes, arbitrary paths, or Git history is
forbidden. Historical Git comparison is a non-live qualification only.

## Fixed Task Pair

Hashing includes every byte and the final LF. Neither task may be rewritten,
regenerated, normalized, trimmed, summarized, exposed in the UI, or transmitted
during this fixation.

| Run | Path | Lane | Bytes | Lines | SHA-256 |
| --- | --- | --- | ---: | ---: | --- |
| Run 1 | `prompts/creator_live_agents_before_after_v0_2_run_1.txt` | `A1_ONLY` | `2713` | `68` | `2ed80098fb169313b13c36dddfd69a3ab487a4fe31d8474889cff7ba441b09e2` |
| Run 2 | `prompts/creator_live_agents_before_after_v0_2_run_2.txt` | `EXACT_A2_ONLY` | `2703` | `56` | `1a67c3677ce8c73b4259317130e689dfefa1827fffe3abe377af861da8ec4bdb` |

Run 1's only dynamic tools are, in order,
`read_candidate_historical_before_v0_2` and
`propose_field_note_candidate`. Run 2 is exact A2 reconnect only. No alternate
Note, scan, fallback, reconstruction, source, or lane exists.

## Fixed Runtime

The future attempt is fixed to:

| Field | Exact value |
| --- | --- |
| Provider | `openai` |
| Account | `ChatGPT` |
| Model | `gpt-5.6-sol` |
| Reasoning | `ultra` |
| Service tier | `priority` |
| Codex CLI | `0.146.0-alpha.3.1` |
| Sandbox | `read-only` |
| Model sandbox network | `false` |
| Provider transport | required |
| Thread | fresh ephemeral thread per Run |
| Working directory | canonical selected repository |

Non-live P0 reads the bundled executable's `--version` identity without
starting app-server or transmitting a task. An unavailable executable or any
version other than `0.146.0-alpha.3.1` makes P0 `NOT_READY`; the content-free
fixed binding may remain visible, but Start remains disabled and no proof
storage, model invocation, or task transmission occurs. Future Start repeats
this check immediately before the proof opener to close post-P0 drift.

Plugins, MCP, apps, hooks, shell, Git, web, arbitrary file reads, attachments,
multi-agent operation, remote plugins, dependency installation, automatic
project-document injection (including repository `AGENTS.md` files), and
arbitrary write/edit/delete capabilities remain absent from Candidate
execution. Candidate `thread/start` fixes `project_doc_max_bytes=0` and
`project_doc_fallback_filenames=[]` plus `features.plugins=false` for both
Runs; `plugins={}` alone is not accepted as a plugin-context isolation gate.
The dedicated fixed-source tool is the sole Run 1 historical-Before disclosure
path.

## One-Attempt and Proof Boundary

Cycle 006 permits exactly one future attempt, zero retries, zero replacements,
no resume after interruption, no alternate proof identity, and no alternate
proof root.

The sole fixed future root is:

```text
/Users/sn/Documents/v13/decision-os-v13-loopkit/
.decision-os/field-notes/proofs/cycle-006
```

It must remain absent throughout non-live qualification and P0. The sole future
identity derivation is:

```text
proof_a7_creator_live_cycle_006_<full_launch_binding_sha256>
```

The complete lowercase 64-character digest is used without truncation.
Duplicate or concurrent requests, refreshes, process restarts, and direct HTTP
replay cannot allocate a second identity. This Delta authorizes deterministic
derivation tests only, not allocation or root creation.

## Launch Binding

The backend recomputes one canonical, content-free binding before any future
proof opening. Its digest covers:

- exact Cycle number/key, Candidate, non-live authorization, absent live-start
  authorization, attempts `1`, retry `0`, replacement `0`, resume prohibited,
  the canonical `cycle-006` proof root, and the full-digest derivation
  `proof_a7_creator_live_cycle_006_<full_launch_binding_sha256>`;
- selected path identity, repository ID, exact final HEAD, local/origin main,
  branch, ahead/behind, tracked/index cleanliness, and no Git operation;
- exact source and installed product-tree identities;
- unchanged Contract filename
  `Decision_OS_Ordinary_User_Path_Contract_v0.1_APPROVED_CANDIDATE.md`,
  `11039`-byte source SHA-256
  `519bd39305af1a3a7cc35e61e1b9cfc742c5723d0cc64d0d970b070d0e65068e`,
  wrapper SHA-256
  `c3de6236a450666d8a8ef59a8f8db303bf4654cc9cb20d6ab816f3066177b11e`,
  interpretation SHA-256
  `7503f4b01c7c05c9ec3aed8855c9fd538c66b9b3b38840f423ec41c2101f4dd7`,
  `INTERPRETATION_ONLY` ordinary authority,
  `IMMUTABLE_INTERPRETATION_ONLY` Guided Intake authority, and exact current
  preparation/freeze receipt and event-chain lineage;
- complete Charter lineage through this v1.0 Delta;
- fixed common Before, source tool, developer instructions, dynamic-tool
  manifest, source-isolation/independence/Post-A1/public projection/public
  bundle/comparison/reduction schema identities;
- exact task pair, lanes, runtime, provider/model-network separation, and
  capability restrictions;
- Candidate v0.2 Gate, generated Witness, and A3 overlay requirements;
- behavior-suite SHA-256
  `655fab6e1de937cc0057af2e5236ce38f07bb19deeb97143655082b2d45522b6`
  and rubric SHA-256
  `553b372340570a211969588fd0114a497846c493171fa9e75494ce8965c705a1`;
- Cycle 005 terminal state and journal/anchor/readback identities;
- immutable Candidate v0.1/v0.2 protected manifests;
- pre-live behavior `NOT_RUN`, comparison `NOT_ESTABLISHED`, no proof identity,
  zero model invocations, zero task transmissions, no real After, and no
  publication authority.

The HTTP caller supplies only `launch_binding_sha256`. A malformed, stale, or
mismatched value fails before proof storage or task/model activity. Private
paths, task text, source bytes, Note/output/Witness text, hidden approvals, and
future proof identity are not projected to the UI.

## Dedicated Production Surface

The dedicated authenticated endpoint is exactly:

```text
POST /api/creator-live/cycles/006/start
```

with the strict body:

```json
{"launch_binding_sha256":"<64 lowercase hexadecimal characters>"}
```

It inherits loopback Host, same Origin, private session, CSRF, JSON content
type/size, duplicate-key, and strict-field protections. It does not reuse
`/api/run`, the Cycle 005 route, ordinary Contract authority, caller-supplied
task/runtime/proof/proof-as-of/retry/replacement/source/lane fields, or any
other execution path.

Before a later separately fixed live authorization, the backend returns
`LIVE_START_AUTHORIZATION_ABSENT` after digest validation and fresh P0/staleness
checks. It opens nothing. The `Start Cycle 006` UI control is visible but
disabled and remains non-actionable under refresh or client-side replay.

The allowlisted UI projection shows Cycle 006, Candidate ID, exact final
revision, common-Before byte count and SHA-256, both Run byte/hash/lane tuples,
the exact runtime tuple, one-attempt/retry/replacement policy, Cycle 005
historical boundary, artifact behavior `NOT_RUN`, comparison
`NOT_ESTABLISHED`, P0 readiness or failure, live authorization `ABSENT`, and
the launch-binding digest. It shows none of the excluded private content.

## Candidate v0.2 Gates

Candidate v0.2 semantics remain exactly those fixed by Delta v0.9:

- installed exact common Before and dedicated fixed-source tool;
- source-isolation and independence receipts;
- public After projection with `after bytes < 20705`;
- B01–B10 each exactly one `PRESENT`;
- public-safety `PASS`;
- generated Witness policy `PASS` and durable exact Witness binding;
- unauthorized metadata absent;
- exact durable Post-A1 readback;
- Run 2 blocked until every Gate passes;
- no repair, retry, second proposal, alternate source/Note, or substitution.

Generic v0.3 A3 remains unchanged. The v0.2 overlay additionally requires one
eligible Candidate, one winner, the designated durable Witness source span,
the durable Witness SHA-256, exact output bytes, and one output occurrence. A
different matching line cannot satisfy the overlay.

The production path is fixed to the existing required provider transport and
these exact Candidate controls, but remains unreachable behind the absent
live-start authorization. Non-live qualification uses only fixtures and fakes
and performs zero provider transport, model invocation, task transmission, or
proof activity. A later explicit live-start decision may make the already
fixed path actionable; it may not alter Candidate semantics or provider
transport semantics.

## Behavior and Comparison Boundary

Artifact behavior is `NOT_RUN` before execution. Fixture qualification may
establish only that the fixed harness behaves deterministically. A future A1–A7
PASS cannot silently establish real behavior preservation; that remains a
separate authorization.

Common-Before comparison remains `NOT_ESTABLISHED` pre-live:

- Lane A / Compactor: every result and metric `NOT_ESTABLISHED`;
- Lane B / Human + AI manual: Before `20705` bytes / `517` lines, After
  `11147` bytes / `359` lines, reduction `9558` bytes, approximately `46.16%`,
  textual/manual restructuring only, behavior `NOT_ESTABLISHED`;
- Lane C / V13: every generated value `NOT_ESTABLISHED`, retry/replacement
  `NOT_AUTHORIZED`.

Lane B After and diff bytes are never Candidate-visible.

The reduction-boundary core statement is fixed exactly:

> These systems are not competitors on one compression axis. They remove waste from different operational surfaces.

The surfaces are RTK/tool-output, Ponytail/implementation,
Compactor/persistent-instruction, OSI/output-handoff, and
V13/iteration-authority. The map is non-ranking. External descriptions remain
`LATER_WEB_VERIFICATION_REQUIRED`; external performance and superiority claims
are forbidden.

## Historical Immutability

Cycle 005 remains permanently:

```text
FAILED / A3_REUSE / A3_EXACT_STRUCTURE_MISSING
```

| Evidence | SHA-256 |
| --- | --- |
| Journal | `1de2e998804f5fb694707846b7deb0dc9d8b5f9cfc6027ad0210ddc270029322` |
| Anchor | `e246757a7ba98849a6b4a694ababf473dc1a98baf1fc1ce0ea7daa3a6e7e8610` |
| Typed readback | `481be90dc8751bda3d7b00714f5a0c650230dffa8974a1332881ce42c127710f` |

Candidate v0.1 and the exact fifteen-file Candidate v0.2 implementation remain
byte-immutable. Cycle 005 terminality creates no Cycle 006 authority. No prior
proof or Candidate may be reopened, retried, replaced, migrated, normalized,
rewritten, reinterpreted, or deleted.

## Qualification and Final P0

Required qualification is fixture/fake/static/process evidence only: focused
Cycle 006 and Candidate suites, route/auth/schema/isolation tests, full default
discovery, JavaScript syntax, Python compilation, JSON parsing,
`git diff --check`, independent Charter and complete-diff review, normal
forward PR merge, exact-final-merge Contract refix, canonical build/install,
source/installed equality, installed-product tests, fake process replay,
installed process qualification, and exact-final-merge P0.

The exact final HEAD is resolved only by the normal merge of this Delta's PR.
After merge, selected `main` and `origin/main` must equal that merge. The same
unchanged Contract must be Forward-only fixed to it, and P0 must bind the exact
current receipt lineage. P0 is observed only through authenticated state/UI or
a read-only snapshot. It never invokes Start.

Final required state is:

```text
Cycle 006: READY / P0
live authorization: ABSENT
Start: disabled
proof root: absent
proof identity: none
model invocation: 0
task transmission: 0
real After: none
artifact behavior: NOT_RUN
comparison result: NOT_ESTABLISHED
retry / replacement: 0 / 0
Cycle 005: unchanged
Candidate v0.1: unchanged
Candidate v0.2: unchanged
exact-final-merge P0: PASS
publication: unauthorized
```

## Rollback

Before proof opening, abandon an unmerged implementation or, after merge, use
a normal Forward revert. Rebuild/install the exact revert, issue another
Forward-only fixation of the unchanged Contract, and restore source/installed
equality plus process qualification. Preserve Cycle 005 and both Candidate
identities. Do not create or remove Cycle 006 proof storage.

If a future proof has opened, this rollback authority no longer applies to the
attempt. Its storage must be preserved under the one-attempt boundary.

## Stop Conditions

Return `HOLD` or `BLOCK` if a protected identity drifts; Candidate v0.2,
generic proof, generic A3, Cycle 005, or ordinary authority semantics would
need modification; non-live qualification would require root creation, model
invocation, task transmission, or a real After; the UI cannot keep Start
disabled; source/installed equality, Contract refix, process qualification, or
exact-final-merge P0 fails; historical state would need mutation; or
publication/external claims become necessary.

## Completion Line

One additive Cycle 006 fixation may reach `READY / P0` at the exact final merge.
No live-start, proof-opening, model, task, behavior, comparison, publication,
release, or external-claim authority is issued by this Delta.
